// This file is automatically updated by the build system
// The actual values come from your .env file and are injected by Vite

export const environment = {
  production: false,
  apiUrl: import.meta.env.VITE_API_URL || 'http://localhost:8000' // Fallback for development
};
